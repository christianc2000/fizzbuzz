* Creé una clase llamada "Exercise" con los métodos que ocuparé
* Dentro de "Exercise" implementé la función para verificar que números son múltiplos de 3 o 5, o ambos y según la lógica retorna un String
* También dentro de "Exercise" realicé una función para recorrer una cantidad, ocupando la función que verificar el múltiplo, imprimiendo de forma automática los valores que son y no son correctos del 1 al 100
* En "Main" creé una instancia de "Exercise" y utilice la función para recorrer una cantidad y mostrar los resultados.
